import { WeakLabelDirective } from './weak-label.directive';

describe('WeakLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new WeakLabelDirective();
    expect(directive).toBeTruthy();
  });
});
